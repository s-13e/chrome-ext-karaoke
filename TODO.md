# TODO (v2.3.1)

> **사이클 성격**: patch — 피드백 기반 개선·최적화 위주. Feature는 다음 사이클(v2.4.0)에서.

## Bugs

_없음_

## Features

_없음 (다음 사이클로 이월)_

## Improvements

### 🔤 용어/문구 정리 (Low effort, High impact)

- [x] **song-info 라벨 변경** — `artist` → `원작자` 등으로 cover와 헷갈리지 않게 단어 수정
- [x] **피드백 용어 통일** — 유저가 "sync mismatch"와 "no lyrics"를 혼용 → 명확한 단어로 수정 (UI 문구 + 피드백 폼 옵션)

### 🎯 온보딩 개선 (Medium effort, High impact)

- [ ] **"가라오케 모드 켜는 방법 모름" 해소** — 첫 진입 가이드/툴팁/하이라이트 등으로 진입 동선 노출 (피드백 다수)

### 🔍 수동 검색 편의성 (Medium effort, Medium impact)

- [x] **영문 타이틀 직접 검색 지원** — 현재는 곡 검색 후에만 영어 타이틀로 재검색 가능 → 수동 검색 진입점에서 바로 영문 입력 가능하게 개선
- [ ] **검색 결과 0건 시 대책** — 미스 매치 + 수동 검색도 실패한 경우 가사를 아예 숨길지, 빈 상태 UI를 보여줄지 정책 결정 후 구현

---

## 📌 추천 구현 순서

이번 patch는 **체감 개선 + 빠른 검증** 위주로 짜는 게 효율적입니다:

1. **용어 정리 2건** — 코드 변경 작고 번역키만 추가하면 됨. 가장 먼저 처리해서 피드백 노이즈 줄이기
2. **수동 검색 영문 입력** — 기존 검색 플로우 확장. 구현 명확하고 매칭 성공률 직접 개선
3. **검색 결과 0건 정책** — 위 2번과 같은 흐름이라 같이 처리하면 컨텍스트 스위칭 적음
4. **온보딩 가이드** — UX 디자인 결정이 필요해 위 셋보다 시간 소요. 중반에 착수

**Stretch goal**: 1~3까지만 끝내도 patch로서 충분히 가치 있음. 4를 함께 묶으면 릴리즈 노트가 풍성해짐.

## 📋 Backlog

- **popup, Song-info 디자인 수정**
- **커버 곡 미스 매치 완화** — 피드백 다수. 원곡/커버 구분 로직 또는 수동 매핑 유도 UX 검토 (선행 리서치 필요). v2.4.0 사이클 후보.
- **피드백 반영 알림** — 사용자가 보낸 피드백이 반영됐을 때 알림. 식별/저장/전송 인프라 필요 → 별도 사이클에서 검토.
- 로컬 단어 현지화 적용
- **아티스트 영문 별칭 로컬 캐시** — 수동 검색에서 검증된 영문 표기를 `chrome.storage.local`에 저장해 다음 영상 감지 때 자동 치환. MusicBrainz가 주는 다중 alias 중 LRCLib에서 실제 히트한 "승자"만 저장하는 구조.
- **영문 alias 다중 후보 순차 시도** — 현재 `extractEnglishAliasFromArtists`가 첫 후보만 반환하는데, 배열로 확장해 LRCLib에 순차 시도. 첫 성공에서 중단. 위 로컬 캐시와 함께 구현.
- **다음 곡 예약/자동재생** — 재생 큐 기능으로 다음 곡 자동 전환. YouTube DOM 제어 및 상태 관리 복잡도가 높아 별도 버전 검토
- **기본 사용 지표 수집** — 어떤 기능을 얼마나 사용하는지 파악 (익명, 옵트인 방식). 정책/권한 이슈로 보류
- **일본어 로마자 가독성 향상** — 띄어쓰기 도입으로 읽기 쉽게 개선
- **접근성(a11y) 개선** — 주요 인터랙티브 요소에 ARIA 라벨·키보드 내비게이션 추가
- **외부 API 레이트 리미팅** — 빠른 영상 전환 시 LRCLib/MusicBrainz 호출 제한
- **에러 리포팅 인프라** — `hidden-source-map` + Sentry(또는 유사 서비스) 연동. 패키징 스크립트에서 `.map` 제외 설정 포함. 소스맵만 켜는 게 아니라 리포팅 서비스까지 함께 구성해야 의미 있으므로 별도 사이클 권장.
- **Tune 파이프라인 AudioWorklet 마이그레이션** — 현재 SoundTouch(피치)가 deprecated `ScriptProcessorNode`를 사용해 UI 스레드에서 돌아감 → Chrome 콘솔 경고 + 스크롤/UI 조작 시 오디오 글리치 가능. AudioWorklet 기반 SoundTouch 대체 필요. Tier 2(ML HD) 착수 시 AudioWorklet 인프라가 필요하므로 그때 같이 처리하면 효율적.
- **아티스트 별칭 캐시 관리 UI** — 옵션 페이지에서 자동 저장된 영문 별칭 목록 확인·수정·삭제. 위 로컬 캐시가 쌓인 뒤 구현. (v2.4.0+ 검토)
