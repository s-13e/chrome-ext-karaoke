# TODO (v2.2.0)

## Bugs

- ~~**5번 비음악 영상 이후 알림 계속 알림** - 현재 5번 비음악 알림 이후에는 알림이 안 떠야하는데, 현재는 알림이 계속 뜨는 버그~~ ✅
- ~~미니모드 -> 기본/영화관 모드로 전환 시, 가사가 안 보이는 버그.(미니 모드일 때는 가사 안 보이게 하기)~~ ✅
- ~~**광고 재생 중 songInfo 오버레이 계속 표시** — adWatcher 콜백이 lyrics만 토글하고 songInfo는 방치되던 버그 수정~~ ✅
- ~~**수동 검색 에러 메시지 언어 변경 후 미갱신** — setError(t(...))가 호출 시점의 번역 문자열을 state에 동결시켜 locale 전환 시 갱신 안 되던 문제. errorState를 `{ kind, key, params }` 형태로 변경하고 렌더 시점에 t() 호출하도록 리팩터~~ ✅

## Features

- **영상에서 음성만 제거하는 로직 추가** - tune에 배치하는 거 괜찮아 보임.

## Improvements

- ~~**튜토리얼 Step1 툴팁 비음악 영상 노출 방지** — 현재 음악 영상 여부와 무관하게 Step1 카라오케 모드 툴팁이 표시됨. 비음악 영상에서는 표시하지 않도록 수정 필요. 호출 시점(`renderMusicNoteButton`)과 음악 판별 시점(`isMusicVideo`)이 다르고 순서가 보장되지 않아 이벤트 기반 연동 필요~~ ✅
- ~~**musicNoteButton의 경우 비음악 영상에서는 css 효과가 없도록**~~ ✅
- ~~**수동 검색에 논 영어 곡의 경우 영어로 검색하는 걸 권장하기** — 0건 + artist/title에 non-ASCII 포함 시 Google 외부 검색 링크 안내 (Level 2)~~ ✅

## 📋 Backlog

- **팝업 디자인 수정**
- **다음 곡 예약/자동재생** — 재생 큐 기능으로 다음 곡 자동 전환. YouTube DOM 제어 및 상태 관리 복잡도가 높아 별도 버전 검토
- **기본 사용 지표 수집** — 어떤 기능을 얼마나 사용하는지 파악 (익명, 옵트인 방식). 정책/권한 이슈로 보류
- **일본어 로마자 가독성 향상** — 띄어쓰기 도입으로 읽기 쉽게 개선
- **접근성(a11y) 개선** — 주요 인터랙티브 요소에 ARIA 라벨·키보드 내비게이션 추가
- **외부 API 레이트 리미팅** — 빠른 영상 전환 시 LRCLib/MusicBrainz 호출 제한
- **프로덕션 소스맵 활성화** — `hidden-source-map` 적용으로 에러 리포팅 개선
- **아티스트 영문 별칭 로컬 캐시** — 수동 검색에서 검증된 영문 표기를 `chrome.storage.local`에 저장해 다음 영상 감지 때 자동 치환. MusicBrainz가 주는 다중 alias 중 LRCLib에서 실제 히트한 "승자"만 저장하는 구조. (v2.3.0+ 검토)
- **영문 alias 다중 후보 순차 시도** — 현재 `extractEnglishAliasFromArtists`가 첫 후보만 반환하는데, 배열로 확장해 LRCLib에 순차 시도. 첫 성공에서 중단. 위 로컬 캐시와 함께 구현. (v2.3.0+ 검토)
- **아티스트 별칭 캐시 관리 UI** — 옵션 페이지에서 자동 저장된 영문 별칭 목록 확인·수정·삭제. 위 로컬 캐시가 쌓인 뒤 구현. (v2.4.0+ 검토)
